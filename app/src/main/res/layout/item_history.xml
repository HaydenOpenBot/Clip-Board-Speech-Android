<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- Doc icon in circle -->
    <ImageView
        android:id="@+id/ivDocIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_icon_circle"
        android:src="@drawable/ic_summary"
        android:padding="8dp"
        android:layout_marginEnd="12dp"
        android:importantForAccessibility="no" />

    <!-- Text block -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginEnd="8dp">

        <TextView
            android:id="@+id/tvHistoryName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/onSurface"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/tvHistoryPreview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/onSurfaceVariant"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="2dp" />

        <TextView
            android:id="@+id/tvHistoryMeta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="@color/outlineVariant"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- More (rename) button -->
    <ImageButton
        android:id="@+id/btnHistoryMore"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/bg_icon_circle"
        android:src="@drawable/ic_more"
        android:padding="6dp"
        android:layout_marginEnd="6dp"
        android:contentDescription="Rename" />

    <!-- Speak button -->
    <ImageButton
        android:id="@+id/btnHistorySpeak"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/bg_icon_circle"
        android:src="@drawable/ic_speak"
        android:padding="6dp"
        android:layout_marginEnd="6dp"
        android:contentDescription="Speak" />

    <!-- Delete button -->
    <ImageButton
        android:id="@+id/btnHistoryDelete"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/bg_icon_error_circle"
        android:src="@drawable/ic_trash"
        android:padding="6dp"
        android:contentDescription="Delete" />

</LinearLayout>
